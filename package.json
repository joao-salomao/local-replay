{
  "name": "local-replay",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server/index.ts",
    "start": "bun run src/server/index.ts",
    "test": "LOG_LEVEL=silent bun test tests/unit tests/integration",
    "test:e2e": "playwright test",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@playwright/test": "^1.49.0",
    "@types/bun": "^1.3.14",
    "@types/qrcode": "^1.5.5"
  }
}
